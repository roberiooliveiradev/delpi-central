---
description: TV Dashboard — um palco canônico editor ≡ apresentação
globs: plugins/tv-dashboard/**/*,plugins/tv-dashboard-presentation/**/*,plugins/public-hub/**/tv-dashboard/**
alwaysApply: false
---

# TV Dashboard — fidelidade editor ↔ apresentação

## Princípio

**Um slide personalizado = um componente de palco.** Editor e TV/prévia não reimplementam fundo/logo/blocos.

**Apresentação = viewer puro** — só chrome (controles, timer, fullscreen) + render. Não decide agregação, filtro, encoding nem default de visual. Dados passam pelo **mesmo** pipeline canônico do editor (`SlideDataResolutionService` → `enrich_blocks` + `applyViewProjection` no cliente).

| Camada | Módulo canônico |
|--------|-----------------|
| Moldura CSS / dual-class | `ComunicadoStageFrame` + `comunicado-stage.css` em `@delpi/plugin-ui` |
| Palco (fundo + logo + blocos) | `RichComunicadoStage` / `RichComunicadoMasterLogo` em `tv-dashboard-presentation` |
| Interação (seleção, handles) | `ComunicadoEditorBlockView` (só editor) |
| Prévia admin / TV pública | `NativeSlideView` → `RichComunicadoStage` |
| Resolução de dados | `SlideDataResolutionService.resolve_blocks` (preview-block **e** present) |
| Projeção KPI/chart/texto | Cliente: `applyViewProjection` / `textViewProjection` — **não** bake `serverProjectionApplied` |
| Ordem na TV (filmstrip) | `presentationSlideOrder.ts` ≡ `is_slide_visible_in_presentation` |
| Dados ao abrir prévia | `preparePreviewNavigation` (flush) + `overlayLiveCustomMessageSlidesOnPreviewPayload` |
| Fit / escala | Prévia de **validação** e TV: `surface=kiosk` (`zoom`). Filmstrip/thumbnail: `surface=preview` (`transform`) |
| External iframe | `ExternalSlideView` em `tv-dashboard-presentation` |
| Enrich prévia ≡ TV | `preview-payload?parity=tv` (default na `PlaylistPreviewPage`); sem flag = JWT do editor |

## Anti-padrões (proibido)

1. Segundo markup de logo master só no composer ou só em `NativeScreens`.
2. Abrir prévia **sem** `flush` do autosave / sem limpar `previewPayloadCache`.
3. Confiar só no `preview-payload` do servidor durante edição (stale) — filmstrip e prévia devem overlay do shell local.
4. Duplicar `PresentationPreview` vs public-hub com lógica de slide diferente (unificar no pacote presentation).
5. **Gráfico/KPI editor ≠ TV/prévia** — confiar em bake do enrichment (`serverProjectionApplied`) e **pular** `applyViewProjection` / `chartDataPolicy` no cliente. O bake do servidor **não** é o visual do editor.
6. Segundo renderer de gráfico (SVG/markup) só na prévia ou só no `/present/` — TV e editor passam por `ChartViewBlockView` → `TvDataSeriesChartWidget` com o **mesmo** `chartOptions` / `chartParts` do bloco.
7. **Label da fonte só no texto** — carregar `listDataRoutes` / `labelCatalog` só para `text`/`shape` e deixar `chart_view`/`kpi_view`/`table_view` com `operationId` na aba Elemento. Usar `useTvDataRouteLabelCatalog` + `isDataBoundEditorBlockType` + `resolveDataSourceLabel` para **qualquer** bloco que usa dados.
8. **Scrollbar da tabela só no portal** — depender do `* { scrollbar-width: thin }` global do portal (public-hub não tem). Estilizar `.delpi-ui-config-table__frame` / `.tdp-table-frame` no kit/native-screens. Wrap `table_view` no editor: `touch-action: pan-x pan-y` (não herdar `none` do bloco).
9. **Lógica de negócio em `PresentationView` / `usePresentationEngine`** além de playback — present não escolhe métrica, período nem encoding.
10. **Índice do filmstrip ≠ ordem da TV** — numerar incluindo slides/seções pausados (present os omite). Usar `buildPresentationOrderIndexBySlideId`.
11. **Prévia de validação com `surface=preview`** — usa `transform` e diverge do kiosk (`zoom`). Validação TV = `kiosk`; só thumbnail/filmstrip usam `preview`.
12. **Assumir enrich JWT = TV** — sem `parity=tv`, o preview-payload usa o Bearer do editor (`authScope=user`).

Incidente jul/2026: prévia com cards vazios/duplicados e layout ≠ editor por payload stale + logo duplicado.  
Incidente jul/2026 (gráfico): pizza/rosca correta no editor, errada em `/preview` e `/present/` porque `applyViewProjection` retornava cedo com `serverProjectionApplied` e ignorava `groupByCategory`.  
Incidente jul/2026 (fonte): Elemento do gráfico mostrava `get_*` enquanto Dados mostrava o label amigável — fetch do catálogo não incluía views.
Incidente jul/2026 (tabela): scrollbar clássica grosso em `/present/` vs thin no editor — estilo só no portal, não no frame da tabela.
Incidente ago/2026 (IDD/present): dual pipeline preview-block vs enrich público + índice filmstrip com pausados → valores e «slide 4» divergentes; present deve ser viewer + `SlideDataResolutionService` + ordem TV.

## O que fazer

- Mudança visual do comunicado → `RichComunicadoStage` (ou `ComunicadoBlockView`).
- Gráfico **e KPI**: encoding em `viewProjection` + `chartDataPolicy` / `kpiProjection`; opções no bloco — nunca pontos/métricas “congelados” do enrichment como fonte de verdade.
- Label da fonte no editor: `useTvDataRouteLabelCatalog` + `resolveDataSourceLabel` / `DataSourceLinkSection` (Elemento e Dados).
- Scroll da tabela: thin no `__frame` / `.tdp-table-frame` (kit + native-screens); teste `tableScrollParityContract.test.ts`.
- Antes de prévia → `preparePreviewNavigation(playlistId)`.
- Save/`presentation_updated` → `reset_comunicado_data_block_cache` (via `notify_presentation_changed`).
- Teste: `RichComunicadoStage.test.tsx`, `overlayLivePreviewPayload.test.ts`, `viewProjection.test.ts` (KPI/chart + `serverProjectionApplied`), `test_slide_data_resolution_parity.py`, `presentationSlideOrder.test.ts`.
