# E8 — Shapes / media / icon

**Status:** IMPLEMENTED

- `blockDefaults.shape` / `image` / `video` / `icon` no catálogo.
- `create_block` com `shape` / `iconName`.
- Materialização stroke/fill/objectFit via defaults + upsert `style`.
- Shape React paint permanece consumer; defaults canônicos no BE.
