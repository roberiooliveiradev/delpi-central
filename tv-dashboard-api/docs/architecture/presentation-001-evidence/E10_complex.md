# E10 — Complexos (gauge / Pin CT / NativeScreens)

**Status:** IMPLEMENTED (reuse)

- `gaugeModel` / `efficiencyPinPresentation` / NativeScreen display já materializados no enrich (FE-BE-002).
- Create de shape `efficiency-pin` via `create_block` + defaults; paint-only no MFE.
- Sem segundo SOT.
