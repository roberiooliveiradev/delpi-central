import { ghostBtnBemClasses } from "@delpi/plugin-ui/index";

/** Ghost dual-class (preferir a `pva-btn--ghost` local quando for shell de botão primário). */
export const PVA_GHOST_BTN = ghostBtnBemClasses("pva");
