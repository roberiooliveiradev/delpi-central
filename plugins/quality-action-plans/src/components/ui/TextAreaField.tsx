import { createDashboardTextAreaField, textAreaFieldPacClasses } from "@delpi/plugin-ui/index";

export const TextAreaField = createDashboardTextAreaField({
  classNames: textAreaFieldPacClasses("pac"),
});
