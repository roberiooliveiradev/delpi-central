import { dataTableBemClasses } from "@delpi/plugin-ui/index";

/** Classes dual `pac-table*` + `.delpi-ui-table*` para tabelas HTML thin do PAC. */
export const PAC_TABLE = dataTableBemClasses("pac");
