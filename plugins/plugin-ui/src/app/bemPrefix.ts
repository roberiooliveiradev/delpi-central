/** Prefixo BEM das demos do catálogo (CSS em catalog.css). */
export const PUC_PREFIX = "puc";

/** Root do app no portal / vite dev — escopo anti-vazamento. */
export const PUC_DASHBOARD_ROOT = "dashboard-plugin-ui";
