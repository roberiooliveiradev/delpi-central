/** @deprecated Use @delpi/plugin-ui — reexport de compatibilidade. */
export {
  triggerFileDownload,
  triggerBlobDownload,
  csvCell,
  buildUtf8CsvBlob,
  sanitizeFilename,
  sanitizeSheetName,
} from "@delpi/plugin-ui";
