import { stateBoxPlaceholderBemClasses } from "@delpi/plugin-ui/index";

/** Placeholder de gráfico/lista — dual `state-box--empty`. */
export const STATE_BOX_EMPTY = stateBoxPlaceholderBemClasses("dp");
