/**
 * Re-export canônico do kit — não divergir da implementação em @delpi/plugin-ui.
 */
export {
  buildLinearTrendValues,
  resolveCalendarBucketFraction,
  withLinearTrendField,
  type BuildLinearTrendOptions,
  type IncompleteBucketMode,
} from "@delpi/plugin-ui/index";
