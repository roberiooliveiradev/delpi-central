import { stateBoxPlaceholderBemClasses } from "@delpi/plugin-ui/index";

/** Placeholder vazio/carregando — classes do kit (não reestilizar `.ds-state-box` no MFE). */
export const EMPTY_STATE_CLASS = stateBoxPlaceholderBemClasses("ds");
