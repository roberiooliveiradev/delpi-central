/**
 * Re-export canônico — chrome em `@delpi/plugin-ui` (`.delpi-ui-segment-toggle*`).
 * Mantido para imports locais existentes no Transformômetro.
 */
export {
  SegmentToggle,
  type SegmentToggleOption,
  type SegmentToggleProps,
} from "@delpi/plugin-ui/index";
