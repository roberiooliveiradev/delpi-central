import { createDashboardStateBox } from "@delpi/plugin-ui/index";

/** Aviso inline canônico do Transformômetro (`ds-state-box` + `.delpi-ui-state-box*`). */
export const StateBox = createDashboardStateBox({ prefix: "ds" });
