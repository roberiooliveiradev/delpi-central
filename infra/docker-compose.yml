services:

  postgres-core:
    image: postgres:15
    container_name: delpi-postgres-core
    restart: unless-stopped

    environment:
      POSTGRES_DB: ${POSTGRES_CORE_DB}
      POSTGRES_USER: ${POSTGRES_CORE_USER}
      POSTGRES_PASSWORD: ${POSTGRES_CORE_PASSWORD}
      POSTGRES_HOST_AUTH_METHOD: scram-sha-256
      TZ: ${TZ}

    volumes:
      - postgres_core_data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./core-api/migrations:/app/migrations

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    networks:
      - delpi-network



  keycloak-db:
    image: postgres:15
    container_name: delpi-keycloak-db
    restart: unless-stopped

    environment:
      POSTGRES_DB: ${POSTGRES_KC_DB}
      POSTGRES_USER: ${POSTGRES_KC_USER}
      POSTGRES_PASSWORD: ${POSTGRES_KC_PASSWORD}
      TZ: America/Sao_Paulo

    volumes:
      - keycloak_data:/var/lib/postgresql/data

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    networks:
      - delpi-network

  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    command: ["start"]
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/${POSTGRES_KC_DB}
      KC_DB_USERNAME: ${POSTGRES_KC_USER}
      KC_DB_PASSWORD: ${POSTGRES_KC_PASSWORD}
      KEYCLOAK_ADMIN: ${KEYCLOAK_ADMIN}
      KEYCLOAK_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}

      KC_HTTP_ENABLED: "${KC_HTTP_ENABLED}"
      KC_HTTP_PORT: "${KC_HTTP_PORT}"
      KC_HTTP_RELATIVE_PATH: "${KC_HTTP_RELATIVE_PATH}"

      KC_HOSTNAME: "${KC_HOSTNAME}"
      KC_HOSTNAME_STRICT: "${KC_HOSTNAME_STRICT}"
      KC_HOSTNAME_STRICT_HTTPS: "${KC_HOSTNAME_STRICT_HTTPS}"

      KC_PROXY: "${KC_PROXY}"
      KC_PROXY_HEADERS: "${KC_PROXY_HEADERS}"
    depends_on:
      - keycloak-db
    networks:
      delpi-network:
        aliases:
          - keycloak   # <- ESSENCIAL pro nginx resolver SEMPRE
    volumes:
      - ./keycloak/themes:/opt/keycloak/themes:ro
      
  core-api:
    build:
      context: ..
      dockerfile: core-api/Dockerfile.prod
    container_name: delpi-core-api
    restart: unless-stopped
    environment:
      FLASK_APP: app.create_app:create_app
      FLASK_ENV: production
      TZ: ${TZ}

      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT}
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      SECRET_KEY: ${SECRET_KEY}

      KEYCLOAK_JWKS_URL: ${KEYCLOAK_JWKS_URL}
      KEYCLOAK_ISSUER: ${KEYCLOAK_ISSUER}
      KEYCLOAK_AUDIENCE: ${KEYCLOAK_AUDIENCE}

      INITIAL_SUPERADMIN_EMAIL: ${INITIAL_SUPERADMIN_EMAIL}
      INITIAL_SUPERADMIN_NAME: ${INITIAL_SUPERADMIN_NAME}

      KEYCLOAK_ADMIN_CLIENT_ID: ${KEYCLOAK_ADMIN_CLIENT_ID}
      KEYCLOAK_ADMIN_CLIENT_SECRET: ${KEYCLOAK_ADMIN_CLIENT_SECRET}
      KEYCLOAK_ADMIN_REALM: ${KEYCLOAK_ADMIN_REALM}
      KEYCLOAK_ADMIN_URL: ${KEYCLOAK_ADMIN_URL}
    depends_on:
      - postgres-core
      - keycloak
    networks:
      - delpi-network

  portal:
    build:
      context: ../portal
      dockerfile: Dockerfile.prod
      args:
        VITE_KC_URL: ${VITE_KC_URL}
        VITE_KC_REALM: ${VITE_KC_REALM}
        VITE_KC_CLIENT_ID: ${VITE_KC_CLIENT_ID}
        VITE_KC_REDIRECT_URI: ${VITE_KC_REDIRECT_URI}
    container_name: delpi-portal
    restart: unless-stopped
    environment:
      TZ: ${TZ}
    depends_on:
      - core-api
    networks:
      - delpi-network

  dashboard-delpi:
    build:
      context: ../plugins/dashboard-delpi
      dockerfile: Dockerfile

    container_name: delpi-dashboard-delpi
    restart: unless-stopped

    environment:
      TZ: ${TZ}

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    networks:
      - delpi-network


  api-delpi:
    build:
      context: ..
      dockerfile: api-delpi/Dockerfile
    container_name: delpi-api-delpi
    restart: unless-stopped

    environment:
      # O CÓDIGO DA API ESPERA ESSES NOMES:
      DB_HOST: ${TOTVS_DB_HOST}
      DB_PORT: ${TOTVS_DB_PORT}
      DB_USER: ${TOTVS_DB_USER}
      DB_PASSWORD: ${TOTVS_DB_PASSWORD}
      DB_DATABASE: ${TOTVS_DB_DATABASE}

      PORT: ${API_DELPI_PORT}
      ENV: ${API_DELPI_ENV}
      JWT_SECRET: ${API_DELPI_JWT_SECRET}

      KEYCLOAK_REALM: ${KEYCLOAK_REALM}
      KEYCLOAK_AUDIENCE: ${KEYCLOAK_AUDIENCE}
      KEYCLOAK_JWKS_URL: ${KEYCLOAK_JWKS_URL}
      KEYCLOAK_ISSUER: ${KEYCLOAK_ISSUER}

      JWT_ALGORITHMS: ${JWT_ALGORITHMS}
      LOG_LEVEL: ${LOG_LEVEL}
      TZ: ${TZ}

    depends_on:
      - keycloak

    networks:
      - delpi-network

  oauth2-proxy-helpdesk:
    image: quay.io/oauth2-proxy/oauth2-proxy:v7.14.0
    container_name: delpi-oauth2-proxy-helpdesk
    restart: unless-stopped
    command:
      - --http-address=0.0.0.0:4180
      - --provider=keycloak-oidc
      - --client-id=oauth2-proxy-helpdesk
      - --client-secret=${OAUTH2_PROXY_CLIENT_SECRET}
      - --redirect-url=https://helpdesk.centraldelpi.com.br/oauth2/callback
      - --oidc-issuer-url=https://centraldelpi.com.br/auth/realms/delpi
      - --scope=openid email profile
      - --email-domain=*
      - --reverse-proxy=true
      - --cookie-secret=${OAUTH2_PROXY_COOKIE_SECRET}
      - --cookie-secure=true
      - --cookie-samesite=lax
      - --cookie-domain=.centraldelpi.com.br
      - --whitelist-domain=.centraldelpi.com.br
      - --set-xauthrequest=true
      - --pass-user-headers=true
      - --skip-provider-button=true
    networks:
      - delpi-network

  gateway:
    build:
      context: ../gateway
      dockerfile: Dockerfile.prod

    container_name: delpi-gateway
    restart: unless-stopped

    ports:
      - "80:80"

    depends_on:
      - portal
      - core-api
      - keycloak
      - api-delpi

    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

    networks:
      - delpi-network



networks:
  delpi-network:
    driver: bridge



volumes:
  postgres_core_data:
  keycloak_data: